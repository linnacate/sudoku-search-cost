# 逐数字复现清单

> 本文列出论文 v8 中的**关键数字**、复现命令与**本包实测值**。
> 标注 ✅ 者为本次打包时**实际跑通并逐位吻合**的项目。

环境：Linux x86-64, gcc/g++ 11, `-O2`（未用 `-march=native`，见下方注记）

---

## 零之前：先取得数据（本包不含题库）

```bash
python out/fetch_data.py     # 下载 tdoku 官方 data.zip 并在本地逐字节重建 5 个数据集
                             # 逐文件校验 MD5，5/5 通过才退出 0；详见 DATA_NOTICE.md
```

## 零、先跑通这条（判定环境是否正常）

```bash
bash build.sh
./bin/exact_bench data/sample5000.txt 5000 30     # 期望 AM = 29.1144
./bin/exact_bench data/sample5000.txt 5000 -1     # 期望 AM = 30.7382
```

| 配置 | 论文值 | 实测值 | 状态 |
|---|---:|---:|---|
| 分层反转（`ALT_REM=30`） | 29.1144 | **29.1144** | ✅ |
| 静态基线（`ALT_REM=-1`） | 30.7382 | **30.7382** | ✅ |
| 改善幅度 | −5.28% | **−5.28%** | ✅ |

> ⭐ 这两个数字复现到小数点后 **4 位**，是本包最强的可复现性证据。

---

## 一、§3 理论结果

### §3.2 引理 1 推论：$\mathrm{crit}(i)>0 \iff \mathrm{minF}(i)=2$

```bash
./bin/verify_thm data/indep600.txt 100
```

| 判据 | 期望 | 实测 |
|---|---:|---:|
| 非对角块 (crit>0, minF≠2) | 0 | **0** ✅ |
| 非对角块 (crit=0, minF=2) | 0 | **0** ✅ |

（论文在 1,335,072 个空格上零反例；此处 100 题为快速验证）

### §3.3 定理 2：规范形式唯一 ⟹ CV 精确 0

```bash
./bin/canon_cv data/indep600.txt 100
```

| 判据 | 期望 | 实测 |
|---|---:|---:|
| 平均 CV（原始 → 规范化后） | 0.xxxx → **0.0000** | **0.1256 → 0.0000** ✅ |
| 组内出现差异的题数 diffCnt | 0 | **0** ✅ |

> 注意：CV=0 **不是**"差异很小"，而是**完全相同**——规范化把整条轨道映射到同一代表元。

---

## 二、§6.3.2 分层反转策略（论文主打结果）

### 主数据集 sample5000

| 配置 | AM | GM | 填格 | 墙钟 µs/题 |
|---|---:|---:|---:|---:|
| 分层（`ALT_REM=30`） | **29.1144** | 20.9580 | 188.68 | 432.77 |
| 静态（`ALT_REM=-1`） | **30.7382** | 21.6869 | 191.18 | 430.24 |

```bash
./bin/exact_bench data/sample5000.txt 5000 30
./bin/exact_bench data/sample5000.txt 5000 -1
```

### 独立种群 top1465（唯一非同源数据集）

| 配置 | AM | GM |
|---|---:|---:|
| 分层 | **3.6758** | 2.6388 |
| 静态 | **3.7891** | 2.6705 |

```bash
./bin/exact_bench data/top1465_clean.txt 1465 30
./bin/exact_bench data/top1465_clean.txt 1465 -1
```

✅ 方向与论文一致（分层更优），且在**独立种群**上成立。

> ⚠️ **墙钟说明**：本表墙钟（432.77 / 430.24 µs）与论文（429.98 µs）有约 0.6% 差异，
> 属环境/负载波动。论文已声明墙钟仅同进程同构建自比有效，**不作跨机依据**。
> 分层策略的墙钟改善很小（−0.31%），其价值在 guesses（−5.28%），不在墙钟。

---

## 三、需第三方源码的数字

以下需先 `bash fetch_third_party.sh` 获取 tdoku / fsss，再 `bash build_3rd.sh`。

| 论文位置 | 数字 | 工具 | 状态 |
|---|---:|---|---|
| §6.2.1 vs fsss | 0.471（少 52.9%）| `h2h/crossarch2` | ⚠️ 未随包验证 |
| §6.2.1 vs fsss2 | 0.564 / 0.417（**跨粒度**，仅参考）| 同上 | ⚠️ |
| §6.2.1 vs tdoku | 0.865（名义比）；BCC 比 **0.294** | 同上 | ⚠️ |
| §6.2.3 vs jczsolve | 0.390（sample5000）/ 0.286（top1465）| `jzrun` | ⚠️ 源码禁止再分发 |
| §6.2.4 ρ 三元组 | 0.123 / 0.095 / **0.080** | `crossarch_report.py` | 见下 |

### §6.2.4 跨架构相关性（可用随包 TSV 独立核验）

> ⚠️ **2026-09-11 修正**：此前文档把两个结果文件写反了。现在：
> * `results/crossarch.tsv` = **sample5000 主数据集，N=5000**（论文 §6.2.4 用的就是这个）
> * `results/top1465_crossarch.tsv` = **top1465 独立种群，N=1465**（单独归档，不是主证据）
>
> 两文件列名相同（`q / our / tdoku / fsss`），但 `q` 是各自数据集内的行下标，
> **不可跨文件对齐**。判别办法：`our` 列均值 —— 5000 题约 30.74（静态基线），1465 题约 3.7891。

`results/crossarch.tsv` 为本方实测的逐题三列数据（our / tdoku / fsss，N=5000）。
审核者可用任意统计工具复算（不依赖 scipy）：

| 求解器对 | Spearman ρ | Kendall τ_b | 最难 1%（50 题）重叠 |
|---|---:|---:|---:|
| 本文 A — tdoku | **+0.1233** | +0.0846 | 5 |
| 本文 A — fsss | **+0.0952** | +0.0646 | 6 |
| **tdoku — fsss** | **+0.0796** | +0.0541 | 3 |

（论文正文取三位小数写作 0.123 / 0.095 / 0.080；τ 写作 0.085 / 0.065 / 0.054。
上表为本方用 `out/recompute_crossarch.py` 的独立复算值，与论文一致到 ±0.0004。）

> ⭐ 论文核心论证：**两个成熟独立的第三方求解器，彼此相关性（0.080）
> 比它们与我们的还低**。这说明难度排序不存在跨求解器共识。

```bash
# 随包复算脚本（纯 numpy + 纯 Python，无需 scipy）
python3 out/recompute_crossarch.py results/
```

> 📌 **数值守恒提示**：`crossarch.tsv` 的 `our` 列是**静态基线（ALT_REM=-1）**这一路配置的逐题
> guesses。论文主打值 29.1144（分层反转 `ALT_REM=30`）需用 `exact_bench` 现跑，**本包不保存该路
> 逐题产物**——这是已知缺口，见 `TEST_REPORT.md`。

---

## 四、编译相关（坑位记录）

以下若忽略会导致**数字错误或结论反转**，均已写入 `build.sh`：

| 编号 | 坑 | 后果 |
|---|---|---|
| 坑 74 | `OurResult` typedef 在 `#ifdef OUR_SOLVER_LIB` 内 | 不带该宏 ⇒ 编译失败或调用约定错 |
| 坑 74b | `main()` 在 `#ifndef OUR_SOLVER_LIB` 内 | 独立求解器变体**不能**加该宏 |
| — | `consolidate.c` 用 g++ 编报 `-fpermissive` | 必须用 **gcc** 编 .o，g++ 仅用于链接 |
| — | tdoku 用 `-march=native` | 可能编出 `avx512vbmi` ⇒ **Illegal instruction**；须 `-mavx2 -mbmi2` |
| 坑 45/58/64/65 | 全局变量题间泄漏、新代码未被执行 | 结论**方向反转**；须加调用计数确认 |

---

## 五、未随包验证的部分（诚实声明）

1. **需第三方的全部数字**（§6.2.1 / §6.2.3）——本包不含第三方源码，未实测。
2. **数据集与 tdoku 官方 `data.zip` 的哈希同一性**——未核验（论文 A.3 已记为待办）。
3. **CPU 微架构型号**——未记录；论文已将其转为明确的适用边界声明。
4. **§6.2.4 的 tdoku 列**仅有本方一侧来源，建议第三方用同一 driver 复跑。
